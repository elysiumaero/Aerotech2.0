# SUDARSHAN-RADHA v1.2 — Feature Test Guide

> **Scope:** Tests every new feature added in v1.2 — Login, Inauguration Mode,
> Hardware Preflight, AES-128-CBC Encrypted Connection, and GPS Server NMEA.
>
> Complete BENCH_TEST.md Section 1–4 (hardware wired, ESP32 up, Mega up, GCS connects)
> before running any test in this document.
>
> **Props OFF throughout. Battery connected only for Sections 5 and 6.**

---

## Prerequisites

### Software dependencies

```bash
# Python 3.10+ required
python3 --version

# Install GCS dependencies
pip install cryptography       # AES encryption (Feature 4)
pip install pytest             # unit test runner

# Verify import works
python3 -c "from cryptography.hazmat.primitives.ciphers import Cipher; print('OK')"
```

### Credential files

Both files must exist before running the GCS.

**GCS/credentials.py** (copy from example)
```bash
cp SUDARSHAN-RADHA/GCS/credentials.py.example SUDARSHAN-RADHA/GCS/credentials.py
# Leave ENCRYPT_ENABLED = False for initial tests
```

**ESP32/…/credentials.h** (copy from example)
```bash
cp "SUDARSHAN-RADHA/ESP32/SUDARSHAN_Bridge/ATLAS_ESP32_Bridge_v2.ino/credentials.h.example" \
   "SUDARSHAN-RADHA/ESP32/SUDARSHAN_Bridge/ATLAS_ESP32_Bridge_v2.ino/credentials.h"
# Edit: set WIFI_SSID, WIFI_PASS
# Leave ENCRYPT_ENABLED 0 for initial tests
```

### Unit tests (run before anything else)

```bash
cd SUDARSHAN-RADHA/GCS
python -m pytest tests/ -v
```

Expected: **29 passed, 0 failed**

If tests fail, do not proceed — fix the error first.

---

## Feature 1 — Login Panel

### 1A — First-run: create account

```
[ ] Delete GCS/auth.json if it exists (fresh-start test):
        rm -f SUDARSHAN-RADHA/GCS/auth.json

[ ] Launch GCS:
        python SUDARSHAN-RADHA/GCS/radha_gcs.py

[ ] A "Create Account" dialog appears (NOT a login dialog)
    [ ] Dialog has three fields: Username, Password, Confirm Password
    [ ] Main window is hidden behind the dialog
    [ ] Cannot interact with main window until dialog is resolved

[ ] Enter mismatched passwords → click CREATE
    [ ] Error message: "Passwords do not match" ✓
    [ ] Dialog stays open ✓

[ ] Enter username: sudarshan   password: test123   confirm: test123
[ ] Click CREATE
    [ ] Dialog closes ✓
    [ ] Main GCS window becomes visible ✓
    [ ] File GCS/auth.json created:
            cat SUDARSHAN-RADHA/GCS/auth.json
        Expected: {"username": "sudarshan", "password_hash": "<64-char hex>"}
    [ ] auth.json is NOT tracked by git:
            git status  →  auth.json does NOT appear ✓
```

### 1B — Login on subsequent runs

```
[ ] Close GCS, relaunch:
        python SUDARSHAN-RADHA/GCS/radha_gcs.py

[ ] Login dialog appears (not create-account)
    [ ] Fields: Username, Password
    [ ] Main window hidden ✓

[ ] Enter wrong password → click LOGIN
    [ ] Error: "Invalid credentials" ✓
    [ ] Dialog stays open ✓

[ ] Enter correct credentials → click LOGIN
    [ ] Dialog closes ✓
    [ ] Main GCS window appears ✓

[ ] Close dialog with window X (without logging in)
    [ ] Application exits entirely (no main window shown) ✓
```

### 1C — Brute-force handling

```
[ ] Attempt login 5× with wrong password
    [ ] No lockout currently enforced (advisory only — document any issues)
    [ ] Each attempt shows "Invalid credentials" without crash ✓
```

---

## Feature 2 — Inauguration Mode

> Requires: GCS connected to ESP32, Mega running, drone DISARMED.
> Props OFF — drone will ARM and send HOVER.

### 2A — Open dialog

```
[ ] In GCS, navigate to FLIGHT tab
[ ] Locate gold "INAUGURATE" button below the KILL button
    [ ] Button is visible and labeled "INAUGURATE" ✓
[ ] Click INAUGURATE
    [ ] InaugurationDialog opens as a modal overlay ✓
    [ ] Main window is not operable while dialog is open ✓
    [ ] Fields visible: Mission Name, T-Minus Seconds, Target Altitude cm ✓
    [ ] Default T-minus: 10   Default altitude: 50 ✓
```

### 2B — Abort before start

```
[ ] Click ABORT without starting countdown
    [ ] Dialog closes ✓
    [ ] No command sent to drone (GCS log unchanged) ✓
```

### 2C — Run countdown (short test)

```
[ ] Enter: Mission Name = "TEST_INAUGURAL"  T-minus = 5
[ ] Click "INITIATE SEQUENCE"
    [ ] Countdown display starts: T-5, T-4, T-3, T-2, T-1, T-0 ✓
    [ ] System bell fires at each step (speaker beep) ✓
    [ ] At T-3: log line "ARM SEQUENCE INITIATED" ✓
              GCS log shows ACK ARM: OK ✓
    [ ] At T-0: log line "LIFT-OFF" ✓
              GCS log shows ACK HOVER: OK ✓
    [ ] Dialog closes automatically after T-0 ✓
    [ ] GCS FLIGHT tab is visible with live telemetry ✓
    [ ] Drone is now ARMED in HOVER mode ✓
    [ ] DISARM manually after test ✓
```

### 2D — Abort during countdown

```
[ ] Start a new countdown (T-minus = 10)
[ ] Wait until T-7 (ARM has NOT yet been sent)
[ ] Click ABORT
    [ ] Countdown stops immediately ✓
    [ ] Log shows "SEQUENCE ABORTED" ✓
    [ ] No ARM command was sent (drone still DISARMED) ✓

[ ] Start another countdown, wait until T-2 (ARM sent, HOVER not yet)
[ ] Click ABORT
    [ ] Log shows "SEQUENCE ABORTED — sending KILL" ✓
    [ ] GCS log shows KILL command sent ✓
    [ ] Drone disarms ✓
```

---

## Feature 3 — Hardware Preflight Tests

> Requires: GCS connected, Mega running, battery connected, Props OFF.
> GPS test (Test 5) requires phone with GPS signal.

### 3A — Preflight tab

```
[ ] In GCS sidebar, click PREFLIGHT tab
    [ ] Tab visible and clickable ✓
    [ ] Left column: 9 test rows with grey status dots ✓
        1. Connectivity
        2. Battery
        3. IMU Level
        4. Sonar
        5. GPS Fix
        6. Dead-Man Switch
        7. Motor FL
        8. Motor FR
        9. Motor RL
        (10. Motor RR)
    [ ] Right column: test output log (empty) ✓
    [ ] RUN ALL button visible ✓
    [ ] CLEAR button visible ✓
    [ ] Individual RUN button next to each test ✓
```

### 3B — Run individual tests

```
TEST 1 — Connectivity:
[ ] Click RUN next to Connectivity
    [ ] Yellow spinner during test ✓
    [ ] After ~2s: green tick, log shows "PING → telemetry received" ✓

TEST 2 — Battery:
[ ] Click RUN next to Battery
    [ ] Green tick if bat_mv ≥ 10,500 ✓
    [ ] Red X if battery below 10,500mV (low battery) ✓
    [ ] Log shows: "bat_mv = XXXXX mV" ✓

TEST 3 — IMU Level:
[ ] Place drone level on flat surface → click RUN
    [ ] Green tick: |roll| < 5° and |pitch| < 5° ✓
[ ] Tilt drone intentionally → click RUN
    [ ] Red X: log shows "roll = X.X° pitch = X.X° (out of tolerance)" ✓

TEST 4 — Sonar:
[ ] Click RUN with drone at normal height
    [ ] Green tick: 0 < alt_cm < 500 ✓
    [ ] Log shows: "alt_cm = XXX cm" ✓

TEST 5 — GPS Fix:
[ ] Connect phone GPS source → wait for 3D FIX (green in GCS)
[ ] Click RUN
    [ ] Green tick: fix=1 and sats ≥ 4 ✓
    [ ] Log: "fix=1  sats=X" ✓
[ ] Without GPS (disconnect phone) → click RUN
    [ ] Red X: "no GPS fix" ✓

TEST 6 — Dead-Man Switch (DMS):
[ ] Click RUN on DMS test
    [ ] Confirmation dialog: "This test will wait 32s without sending PING..." ✓
    [ ] Click CONFIRM
    [ ] Test waits silently for ~32s ✓
    [ ] Mode changes to FAILSAFE in GCS telemetry ✓
    [ ] Green tick logged ✓
    [ ] Send HOVER from GCS to exit FAILSAFE after test ✓
```

### 3C — Motor tests (Props OFF mandatory)

```
TEST 7–10 — Motor FL, FR, RL, RR:
[ ] Click RUN on Motor FL
    [ ] Confirmation dialog: "CONFIRM PROPS ARE REMOVED" appears ✓
    [ ] Click CANCEL → test does not run ✓
    [ ] Click RUN again → click CONFIRM in dialog
    [ ] GCS sends MOTOR_TEST {"motor":"FL","throttle":1100,"duration_ms":2000}
    [ ] Mega Serial Monitor: [CMD] MOTOR_TEST  motor=FL
    [ ] Front-Left motor spins for ~2s then stops ✓
    [ ] GCS log: ACK MOTOR_TEST: OK  FL ✓
    [ ] Green tick on Motor FL ✓

[ ] Repeat for FR, RL, RR — verify each correct motor spins ✓
```

### 3D — RUN ALL and ARM interlock

```
[ ] Click CLEAR → all dots reset to grey ✓
[ ] Click RUN ALL
    [ ] Tests run sequentially 1→9 ✓
    [ ] Each spinner turns yellow then green/red ✓
    [ ] Motor tests pause for confirmation dialog each time ✓

ARM INTERLOCK:
[ ] With critical tests (1–4) all passing (green):
    [ ] Click ARM in FLIGHT tab → ARM proceeds normally ✓

[ ] Click CLEAR, then click ARM without running tests
    [ ] Warning dialog: "Preflight critical tests not passed" ✓
    [ ] ARM is NOT sent ✓
    [ ] Can dismiss warning and still ARM (advisory, not hard block)

[ ] Run tests 1–4, leave test 5+ not run
    [ ] ARM proceeds without warning (5–10 are advisory only) ✓
```

---

## Feature 4 — AES-128-CBC Encrypted Connection

> This test requires reflashing the ESP32 with ENCRYPT_ENABLED 1.
> Keep encryption DISABLED for all other tests and only enable here.

### 4A — Generate and set AES key

```bash
# Generate a random 16-byte key (32 hex chars):
python3 -c "import os; print(os.urandom(16).hex())"
# Example output: a3f1b2c4d5e6f7081920a1b2c3d4e5f6
```

```
[ ] Copy the generated key (exactly 32 hex characters)

[ ] Edit SUDARSHAN-RADHA/GCS/credentials.py:
        AES_KEY = "a3f1b2c4d5e6f7081920a1b2c3d4e5f6"   ← your key
        ENCRYPT_ENABLED = True

[ ] Edit credentials.h:
        #define AES_KEY_HEX  "a3f1b2c4d5e6f7081920a1b2c3d4e5f6"  ← same key
        #define ENCRYPT_ENABLED 1

[ ] Confirm keys are IDENTICAL character-for-character ✓
[ ] Neither file is committed (git status shows them as untracked/ignored) ✓
```

### 4B — Flash and verify

```
[ ] In Arduino IDE: open ATLAS_ESP32_Bridge_v2.ino
[ ] Compile and flash to ESP32
[ ] Open Serial Monitor (115200 baud)
    Expected line: [CRYPTO] AES-128-CBC ENABLED  key_len=16

[ ] Launch GCS:
        python SUDARSHAN-RADHA/GCS/radha_gcs.py

[ ] GCS connects to ESP32 → status shows CONNECTED ✓
[ ] GCS log shows normal telemetry (decrypted transparently) ✓
[ ] Capture raw TCP traffic (Wireshark or netcat):
        nc 192.168.4.1 5760
    → Data should appear as base64 blobs, NOT readable JSON ✓
```

### 4C — Key mismatch (negative test)

```
[ ] Change only GCS credentials.py to a different key
[ ] Relaunch GCS and connect
    [ ] GCS log shows JSON parse errors or garbled output ✓
    [ ] Telemetry NOT displayed (decryption fails silently, line skipped) ✓
[ ] Restore correct key → reconnect → works again ✓
```

### 4D — Fallback to plaintext

```
[ ] Set ENCRYPT_ENABLED = False in credentials.py
[ ] Set #define ENCRYPT_ENABLED 0 in credentials.h and reflash ESP32
[ ] Reconnect GCS → works normally without encryption ✓
[ ] Run all unit tests again — all 29 pass ✓
```

> **Leave ENCRYPT_ENABLED = 0 / False after this test section**
> so the rest of the bench tests work without reflashing.

---

## Feature 5 — GPS Server (Metrologic) NMEA Parsing

> Requires: GPS Server by Metrologic app (Android/iOS) installed on phone.
> Phone must be connected to SUDARSHAN_AP WiFi before starting GPS Server.

### 5A — App configuration

```
[ ] On phone: Settings → WiFi → connect to SUDARSHAN_AP (password: radha2026)
[ ] Open GPS Server by Metrologic
[ ] In-app settings:
        Mode    → TCP Client    (NOT Server)
        Host    → 192.168.4.1
        Port    → 5762
        Output  → NMEA
[ ] Enable the GPS source within the app (allow location permission)
```

### 5B — Connection test

```
[ ] Start GPS Server app streaming
    [ ] ESP32 Serial Monitor: [PHONE] Connected from 192.168.4.X ✓
    [ ] ESP32 Serial Monitor shows NMEA lines being parsed:
            [GPS ] GGA  fix=1  sats=8  lat=28.613900  lon=77.209000  alt=216.0
            [GPS ] RMC  valid  hdg=42.3
    [ ] GCS GPS panel updates (may take a few seconds):
        [ ] LAT: XX.XXXXXX  (not 0.000000) ✓
        [ ] LON: XX.XXXXXX  (not 0.000000) ✓
        [ ] FIX: 3D FIX (green) or NO FIX if outdoors GPS not locked ✓
        [ ] SATS: a number > 0 ✓
        [ ] HEADING: XX.X° ✓
```

### 5C — Indoor no-fix test

```
[ ] Test indoors where GPS signal is weak
    [ ] GCS GPS panel shows: FIX: NO FIX (red) ✓
    [ ] SATS: low number or 0 ✓
    [ ] LAT/LON may show 0.000000 (no fix = no valid position) ✓
    [ ] System continues to function — just no GPS-dependent features ✓
```

### 5D — NMEA checksum validation

```
[ ] In ESP32 Serial Monitor, look for checksum error lines if any
    [ ] Malformed NMEA lines silently discarded (no crash) ✓
    [ ] Valid lines parsed and forwarded to FC ✓
```

### 5E — Phone disconnect

```
[ ] Stop GPS Server app (or disconnect phone from WiFi)
    [ ] ESP32 Serial Monitor: [PHONE] Disconnected ✓
    [ ] GCS log: ESP32: PHONE_DISCONNECTED (amber) ✓
    [ ] GCS GPS panel freezes on last known values ✓
    [ ] System continues operating — FC holds last GPS data ✓
```

### 5F — Reconnect

```
[ ] Restart GPS Server app and reconnect
    [ ] ESP32 accepts new connection ✓
    [ ] GPS panel resumes updating ✓
    [ ] No restart of GCS or ESP32 needed ✓
```

---

## Regression Check — Existing Features

After all v1.2 feature tests, confirm v1.0 features still work:

```
[ ] ARM / DISARM ✓
[ ] HOVER command ✓
[ ] LAND command ✓
[ ] KILL command (confirmation dialog) ✓
[ ] OVERRIDE via GCS sliders ✓
[ ] PRESET path (3 segments) ✓
[ ] DMS 30s timeout → FAILSAFE ✓
[ ] Battery alarm at 10,500mV (label turns orange) ✓
[ ] Auto-reconnect: disconnect GCS → reconnect → telemetry resumes ✓
[ ] Telemetry ROLL/PITCH/YAW update live ✓
[ ] Sonar ALT updates live ✓
```

---

## Final Unit Test Run

```bash
cd SUDARSHAN-RADHA/GCS
python -m pytest tests/ -v --tb=short
```

Expected: **29 passed, 0 failed, 0 errors**

---

## Test Sign-Off

```
Tester name   : ________________________________
Date          : ________________________________
Firmware:
    ESP32 Bridge : ATLAS_ESP32_Bridge_v2   commit: __________
    Mega FC      : SUDARSHAN_FC            commit: __________
    RADHA GCS    : radha_gcs.py v1.2       commit: __________

Feature test results:
    F1 Login Panel          :  PASS / FAIL / PARTIAL
    F2 Inauguration Mode    :  PASS / FAIL / PARTIAL
    F3 Hardware Preflight   :  PASS / FAIL / PARTIAL
    F4 AES Encryption       :  PASS / FAIL / PARTIAL
    F5 GPS Server NMEA      :  PASS / FAIL / PARTIAL
    Regression              :  PASS / FAIL / PARTIAL

Unit tests  : 29 / 29  passed

Notes:
____________________________________________________________
____________________________________________________________

Cleared for full bench test (BENCH_TEST.md):  YES / NO
```
